// 성별(Gender)
export const GENDER = Object.freeze({
  MALE: "male",
  FEMALE: "female",
});

// 회원 타입(User Role)
// export const USER_ROLE = Object.freeze({
//   USER: "user",
//   ADMIN: "admin",
// });

// 주차장 유무
export const PARKING = Object.freeze({
  YES: "yes",
  NO: "no",
});

// 입장료 유무
export const ADMISSION = Object.freeze({
  FREE: "free",
  PAID: "paid",
});

// 예약 유무
export const RESERVATION = Object.freeze({
  REQUIRED: "required",
  NOT_REQUIRED: "not_required",
});

// 지역화폐 사용 가능한지
export const LOCAL_PAY = Object.freeze({
  AVAILABLE: "available",
  NOT_AVAILABLE: "not_available",
});
